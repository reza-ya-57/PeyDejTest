@using PeyDej.Tools
@model IEnumerable<PeyDej.Models.Bases.Machine>


@(Html
    .Grid(Model)
    .Build(columns =>
    {
        columns.Add(model => model.SerialNumber).Titled(@Html.DisplayNameFor(model => model.SerialNumber));
        columns.Add(model => model.Model).Titled(@Html.DisplayNameFor(model => model.Model));
        columns.Add(model => model.Company).Titled(@Html.DisplayNameFor(model => model.Company));
        columns.Add(model => model.Country).Titled(@Html.DisplayNameFor(model => model.Country));
        columns.Add().Titled("ویرایش").Encoded(false).RenderedAs(model =>
            Html.ActionLink("ویرایش", "Edit", new { id = model.Id },
                new { @class = "btn btn-warning btn-sm" })
            );
        columns.Add().Titled("ویرایش").Encoded(false).RenderedAs(model =>
            Html.ActionLink("جزییات", "Details", new { id = model.Id },
                new { @class = "btn btn-success btn-sm" }));
        columns.Add().Titled("حذف").Encoded(false).RenderedAs(model =>
            PeyDejHelper.ActiveLinkAjax(
                $"javascript:DeleteItem('{model.Id}');", "حذف", "btn btn-danger btn-sm")
            );
    })
    .Empty("هیچ ایتمی وجود ندارد")
    .Sortable()
    .Filterable()
    .Using(GridFilterMode.Row)
    .Pageable(pager =>
    {
        pager.PageSizes = new Dictionary<Int32, String> { { 10, "10" }, { 20, "20" }, { 50, "50" }, { 0, "همه" } };
        pager.ShowPageSizes = true;
        pager.PagesToDisplay = 5;
        pager.CurrentPage = 1;
        pager.RowsPerPage = 10;
    })
    .Css("table table-secondary table-striped gCol")
    )